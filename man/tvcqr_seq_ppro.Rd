% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tvcqr_seq_ppro.R
\name{tvcqr_seq_ppro}
\alias{tvcqr_seq_ppro}
\title{Fast screened time-varying coefficient quantile regression}
\usage{
tvcqr_seq_ppro(x, y, tau = 0.5, h = NULL, threshold_factor = 1e-05)
}
\arguments{
\item{x}{Finite numeric predictor matrix. Rows must be in time order.}

\item{y}{Numeric response vector with length `nrow(x)`.}

\item{tau}{Quantile level strictly between zero and one.}

\item{h}{Positive bandwidth. Defaults to `nrow(x)^(-1/5)`.}

\item{threshold_factor}{Positive finite multiplier for the initial screening
threshold. The default is `1e-5`.}
}
\value{
A list containing `theta_ll_est`, `beta_full_est`, `time_index`,
  and `h`.
}
\description{
Fits time-varying coefficient quantile regression on the ordered grid
`seq_len(n) / n` using the exact screened warm-start ppro solver.
}
\examples{
set.seed(2)
n <- 80
time_index <- seq_len(n) / n
x <- matrix(rnorm(n * 2), ncol = 2)
y <- sin(2 * pi * time_index) + 0.5 * x[, 1] + rnorm(n)
fit <- tvcqr_seq_ppro(x, y)
head(fit$theta_ll_est)
}
