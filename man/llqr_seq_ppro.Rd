% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llqr_seq_ppro.R
\name{llqr_seq_ppro}
\alias{llqr_seq_ppro}
\title{Fast screened local linear quantile regression}
\usage{
llqr_seq_ppro(
  x,
  y,
  tau = 0.5,
  z = NULL,
  h = NULL,
  kernel = c("gaussian", "epanechnikov"),
  threshold_factor = 0.1
)
}
\arguments{
\item{x}{Numeric predictor vector.}

\item{y}{Numeric response vector with the same length as `x`.}

\item{tau}{Quantile level strictly between zero and one.}

\item{z}{Numeric evaluation points. Defaults to `x`.}

\item{h}{Positive bandwidth. When `NULL`, a kernel-specific default is used.}

\item{kernel}{Either `"gaussian"` or `"epanechnikov"`.}

\item{threshold_factor}{Positive finite multiplier for the initial screening
threshold. The default is `0.1`.}
}
\value{
A list containing `ll_est`, `d_ll_est`, `z`, and `h`.
}
\description{
Fits one-dimensional local linear quantile regression along an ordered
evaluation grid using the exact screened warm-start ppro solver.
}
\examples{
set.seed(1)
x <- sort(runif(80))
y <- 1 + 2 * x^2 + rnorm(80)
fit <- llqr_seq_ppro(x, y, z = seq(0.1, 0.9, length.out = 20),
                     kernel = "epanechnikov")
head(fit$ll_est)
}
